"""Core utilities and shared building blocks for NPBB pipelines.

This package is intentionally lightweight and dependency-minimal. Higher-level
pipelines (ETL, report generation, etc.) should depend on `npbb.core`, not the
other way around.
"""

