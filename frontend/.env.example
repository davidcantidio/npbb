# Base URL do backend (FastAPI)
# Padrao recomendado em dev local: usa proxy do Vite (/api -> http://localhost:8000).
VITE_API_BASE_URL=/api

# Patrocinados: true = usa API /sponsorship no backend; omita ou false = localStorage no navegador.
VITE_SPONSORSHIP_USE_API=true
