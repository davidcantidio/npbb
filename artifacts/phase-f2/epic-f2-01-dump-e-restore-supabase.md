# Artifact - EPIC-F2-01 Dump e Restore Supabase

## Estado Atual

Status do artifact: `pending-execution`.

Este arquivo existe para concentrar a evidencia minima do fluxo de dump, restore e alembic da Fase 2.

## Preflight

- Work order registrado em `artifacts/phase-f2/work-order-and-decision.md`.
- `SUPABASE_DATABASE_URL`: ainda nao executado.
- `LOCAL_DATABASE_URL`: ainda nao executado.
- Aprovacao humana: pendente.
- Rollback state: manter Supabase como fonte de verdade ate validacao completa.

## Manifest do Dump

- Nome do arquivo: nao executado.
- Timestamp UTC: nao executado.
- Tamanho do arquivo: nao executado.
- Hash SHA256: nao executado.

## Restore

- Container de banco local: nao executado.
- Extensoes base: nao verificadas.
- Resultado do `pg_restore`: nao executado.

## Alembic

- Resultado do `alembic upgrade head`: nao executado.
- Revision final observada: nao executado.

## Observacoes

- Enquanto este artifact estiver em `pending-execution`, a decisao da fase permanece `hold`.
